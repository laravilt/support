/**
 * `@laravilt/support/types` — imported by the panel layouts (`BreadcrumbItemType`).
 * The Vue package has no such module (it resolved through the host app); the React twin re-exports
 * the React starter kit's navigation types under both names.
 */
export type { BreadcrumbItem, BreadcrumbItem as BreadcrumbItemType, NavItem } from '@/types';
